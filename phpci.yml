build_settings:
  verbose: true
  
setup:
    shell:
        action: "id"
    shell:
        action: "composer config --global github-protocols https -vvv"
    shell:
        action: "composer install -vvv"
