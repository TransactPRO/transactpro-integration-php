build_settings:
    verbose: true
  
setup:
    shell:
        - echo "Running 'setup' stage"
        - mkdir -p build/logs
    composer:
        action: install
        
test:
    shell:
        - echo "Running 'test' stage"
    php_unit:
        coverage: build/logs
        
complete:
    email:
        default_mailto_address: vl@tpro.lv
    shell:
        - echo "Running 'complete' stage (emails)"
        
success:
    shell:
        - echo "Running 'success' stage"
        
failure:
    shell:
        - echo "Running 'failure' stage"
