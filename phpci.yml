setup:
    composer:
        action: "config --global github-protocols https"
    composer:
        action: "self-update"
        prefer_dist: true
    composer:
        action: "install"
        prefer_dist: true
