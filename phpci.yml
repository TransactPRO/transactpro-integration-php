setup:
    composer:
        action: "config --global github-protocols https"
    composer:
        action: "self-update"
        prefer_dist: false
    composer:
        action: "install"
        prefer_dist: false
