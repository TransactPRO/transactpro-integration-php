build_settings:
    verbose: true
  
setup:
    composer:
        action: install
    shell:
        - mkdir -p build/logs
        - echo "test2 phpci hook"
        
test:
    php_unit:
        coverage: build/logs
